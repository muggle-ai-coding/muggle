---
import { getCollection } from 'astro:content';
import BaseLayout from '../../../layouts/BaseLayout.astro';

// Get all Chinese blog posts
const allPosts = await getCollection('blog', ({ data }) => {
  return data.lang === 'zh';
});

// Sort posts by date (newest first)
const sortedPosts = allPosts.sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);

// Get category from URL parameter
const url = Astro.url;
const categoryFilter = url.searchParams.get('category');

// Filter posts by category if specified
const filteredPosts = categoryFilter
  ? sortedPosts.filter(post => post.data.category === categoryFilter)
  : sortedPosts;

// Category display names
const categoryNames: Record<string, string> = {
  cloud: '☁️ 雲端自動化',
  desktop: '💻 桌面自動化',
  web: '🌐 網頁開發',
  'core-concepts': '📚 核心概念',
  'more-cases': '💡 更多案例'
};
---

<BaseLayout 
  title="所有案例 - 麻瓜的 AI 編程之路"
  description="瀏覽所有自動化教學和案例"
  lang="zh"
>
  <div class="blog-list">
    <div class="container">
      <div class="page-header">
        <h1>
          {categoryFilter ? categoryNames[categoryFilter] : '所有案例'}
        </h1>
        <p>
          {categoryFilter 
            ? '瀏覽此分類下的教學'
            : '瀏覽所有自動化教學和範例'
          }
        </p>
      </div>

      {categoryFilter && (
        <div class="filter-info">
          <a href="/zh/blog" class="clear-filter">
            ← 顯示所有分類
          </a>
        </div>
      )}

      {filteredPosts.length === 0 ? (
        <div class="no-posts">
          <p>此分類尚無文章。</p>
          <a href="/zh/blog" class="btn btn-primary">查看所有文章</a>
        </div>
      ) : (
        <div class="posts-grid">
          {filteredPosts.map((post) => (
            <article class="post-card">
              <div class="post-category">
                {categoryNames[post.data.category]}
              </div>
              <h2>
                <a href={`/zh/blog/${post.slug.replace('zh/', '')}`}>
                  {post.data.title}
                </a>
              </h2>
              <p class="post-description">
                {post.data.description}
              </p>
              <div class="post-meta">
                <time datetime={post.data.pubDate.toISOString()}>
                  {post.data.pubDate.toLocaleDateString('zh-TW', {
                    year: 'numeric',
                    month: 'long',
                    day: 'numeric'
                  })}
                </time>
              </div>
              <a href={`/zh/blog/${post.slug.replace('zh/', '')}`} class="read-more">
                閱讀更多 →
              </a>
            </article>
          ))}
        </div>
      )}
    </div>
  </div>
</BaseLayout>

<style>
  .blog-list {
    padding: 3rem 0;
  }

  .page-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .page-header h1 {
    font-size: 2.5rem;
    margin-bottom: 1rem;
  }

  .page-header p {
    color: var(--color-text-light);
    font-size: 1.125rem;
  }

  .filter-info {
    margin-bottom: 2rem;
  }

  .clear-filter {
    color: var(--color-primary);
    font-weight: 600;
  }

  .posts-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
    gap: 2rem;
  }

  .post-card {
    background: var(--color-bg);
    border: 1px solid var(--color-border);
    border-radius: 0.75rem;
    padding: 2rem;
    transition: var(--transition);
    display: flex;
    flex-direction: column;
  }

  .post-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
    border-color: var(--color-primary);
  }

  .post-category {
    font-size: 0.875rem;
    color: var(--color-primary);
    font-weight: 600;
    margin-bottom: 0.75rem;
  }

  .post-card h2 {
    font-size: 1.5rem;
    margin-bottom: 0.75rem;
  }

  .post-card h2 a {
    color: var(--color-text);
  }

  .post-card h2 a:hover {
    color: var(--color-primary);
  }

  .post-description {
    color: var(--color-text-light);
    margin-bottom: 1rem;
    flex-grow: 1;
  }

  .post-meta {
    color: var(--color-text-light);
    font-size: 0.875rem;
    margin-bottom: 1rem;
  }

  .read-more {
    color: var(--color-primary);
    font-weight: 600;
    margin-top: auto;
  }

  .no-posts {
    text-align: center;
    padding: 4rem 2rem;
  }

  .no-posts p {
    color: var(--color-text-light);
    font-size: 1.125rem;
    margin-bottom: 2rem;
  }

  @media (max-width: 768px) {
    .posts-grid {
      grid-template-columns: 1fr;
    }

    .page-header h1 {
      font-size: 2rem;
    }
  }
</style>

